# v0.1.5 flash package parts

Windows PowerShell blocked the older zip scripts. This folder rebuilds the full zip:

```powershell
cd flash-packages\v015-parts
.\EXPAND.ps1
```

Then use the generated `flash-packages\pinebuds-cros-v0.1.5.zip` (or LATEST).
